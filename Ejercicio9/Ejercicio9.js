const topics = ['JavaScript', 'Variables', 'funciones', 'condicionales', 'bucles'];
console.log('topics',topics)
const MAY=topics.map(topic=>topic.toLocaleUpperCase())
console.log(MAY)
MAY.reverse();
console.log(MAY)